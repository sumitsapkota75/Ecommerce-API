package constants

const (
	// DBTransaction  -> database transaction handle set at router context
	DBTransaction = "db_trx"

	// Claims -> authentication claims
	Claims = "Claims"

	// UID -> authenticated user's id
	UID = "UID"

	// ClientID -> authenticated user's client id
	ClientID = "ClientID"

	//Email Address
	Email = "Email"
)
